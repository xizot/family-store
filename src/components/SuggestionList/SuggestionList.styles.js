import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  suggestListContent: {
    position: 'relative',
    padding: '0px 42px 15px',
  },
  suggestSlider: {
    overflow: 'hidden',
    padding: '0px 2px',
  },
  noProduct: {
    fontSize: 20,
    color: '#999',
    fontWeight: 'bold',
    textAlign: 'center',
  },
}));
