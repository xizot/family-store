import classes from "./Header.module.scss";

const Header = () => {
    return (
        <header>
            <h3 className={classes.header__title}>Header</h3>
        </header>
    );
};
export default Header;
